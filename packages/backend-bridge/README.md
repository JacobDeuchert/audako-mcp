# Backend Bridge

Service that will mediate between the chat UI and the MCP server (authentication, session handling, proxying requests).

Next steps: choose a framework (Express/Fastify/etc.), sketch API surface, and wire it to the MCP client.
